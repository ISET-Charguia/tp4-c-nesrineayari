#include<stdio.h>
int main (){
	int t[20],n,i,j,q;
	do{
		printf("donner n:");
		scanf("%d",&n);
	}while(n<0);
	i=0;
	do{
		q=n%2;
		t[i]=q;
		n=n/2;
		i++;}while(n!=0);
	for(j=i-1;j>=0;j--)
		printf("%d",t[j]);
	return 0;
}
